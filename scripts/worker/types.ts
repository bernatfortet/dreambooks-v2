/**
 * Shared types for the scraping worker.
 * 
 * Note: Discovery type is defined in lib/scraping/types.ts
 */

// Re-export Discovery for convenience
export type { Discovery } from '../../lib/scraping/types'
